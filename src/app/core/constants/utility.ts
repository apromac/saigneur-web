import {Campagne} from '../../data/schemas/campagne';
import {UsersModel} from '../../data/schemas/users.model';

export class Utility {
  static loggedUser: UsersModel;
  static CURRENTCAMPAGNE: Campagne;
}
